/// {@template auth_repository}
/// A repository that handles the whole authentication system for the app.
/// {@endtemplate}
class AuthRepository {
  /// {@macro auth_repository}
  const AuthRepository();
}
